@model IEnumerable<Entities.Service>
@{
    ViewData["Title"] = "Index";
}

<h1>Услуги</h1>
<table class="table">
    <tr>
        <th class="t">Id</th>
        <th class="t">Название</th>
    </tr>

    @foreach (var service in Model)
    {
        <tr>
            <td class="t">@service.Id</td>
            <td class="t">@service.Name</td>
            <td>
                
                <a asp-controller="ServiceRates" asp-action="ShowRates" asp-route-id="@service.Id" class="button-my">Показать тарифы</a>
                <br />
                <a asp-controller="ServiceRates" asp-action="AddRate" asp-route-id="@service.Id" class="button-my">Добавить тариф</a>
            </td>
        </tr>
    }



</table>