@model IEnumerable<Entities.Service>
@{
    ViewData["Title"] = "Index";
}

<h1>Услуги</h1>
<table class="table">
    <tr>
        <th class="t">Id</th>
        <th class="t">Название</th>
    </tr>

    @foreach (var service in Model)
    {
        <tr>
            <td class="t">@service.Id</td>
            <td class="t">@service.Name</td>
            <td>
                
                <a asp-controller="ServiceReadings" asp-action="ShowReadings" asp-route-id="@service.Id" class="button-my">Показания</a>
                <br />
                <a asp-controller="ServiceReadings" asp-action="AddReading" asp-route-id="@service.Id" class="button-my">Добавить показания</a>
                <br />
                <a asp-controller="ServiceReadings" asp-action="ShowDebt" asp-route-id="@service.Id" class="button-my">Задолжность</a>
            </td>
        </tr>
    }



</table>